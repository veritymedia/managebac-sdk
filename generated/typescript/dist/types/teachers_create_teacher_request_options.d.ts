/**
 * Request directives applied during the create operation.
 */
export interface TeachersCreateTeacherRequestOptions {
    /**
     * If true, a welcome email is enqueued for the newly created teacher.
     */
    sendWelcomeEmail?: boolean;
}
