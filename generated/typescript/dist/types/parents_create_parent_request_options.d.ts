/**
 * Request directives applied during the create operation.
 */
export interface ParentsCreateParentRequestOptions {
    /**
     * If true, a welcome email is enqueued for the newly created parent.
     */
    sendWelcomeEmail?: boolean;
}
