/**
 * Request directives applied during the create operation.
 */
export interface StudentsCreateStudentRequestOptions {
    /**
     * If true, a welcome email is enqueued for the newly created student.
     */
    sendWelcomeEmail?: boolean;
}
