{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "managebac": {
      "type": "local",
      "command": ["node","{env:MCP_ABS_PATH}", "--scope", "read"],
      "enabled": true,
      "environment": { "MANAGEBAC_API_KEY": "{env:MANAGEBAC_API_KEY}" }
    }
  }
}
