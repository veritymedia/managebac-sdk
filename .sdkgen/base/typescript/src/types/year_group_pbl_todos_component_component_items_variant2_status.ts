/**
 * Student submission state for the deadline; null when the deadline has no dropbox.
 */
export type YearGroupPblTodosComponentComponentItemsVariant2Status = "Pending" | "Submitted" | "Late" | (string & {});
